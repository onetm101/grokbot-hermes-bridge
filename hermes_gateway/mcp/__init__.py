"""Hermes MCP gateway subpackage.

Public surface:

* :func:`hermes_gateway.mcp.config.load_config` — fail-closed runtime configuration;
* :func:`hermes_gateway.mcp.gateway.build_app` — built hardened ASGI application;
* :func:`hermes_gateway.mcp.server.build_hermes_server` — the MCP ``FastMCP`` server.
"""
